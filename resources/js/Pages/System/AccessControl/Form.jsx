import React from "react";
import { Head, Link, useForm } from "@inertiajs/react";
import { ArrowLeft, Save, UserPlus } from "lucide-react";
import AdminLayout from "../../../Layouts/AdminLayout";

function Field({ label, error, children }) { return <label className="block"><span className="mb-2 block text-xs font-semibold text-slate-600">{label}</span>{children}{error && <span className="mt-1.5 block text-xs font-semibold text-rose-600">{error}</span>}</label>; }

export default function Form({ mode = "create", userRecord = {}, roles = [], submitUrl, backUrl }) {
    const isEdit = mode === "edit";
    const { data, setData, post, put, processing, errors } = useForm({ nik: userRecord.nik || "", email: userRecord.email || "", role: userRecord.role || roles.find((role) => role.name === "viewer")?.name || "", password: "", password_confirmation: "" });
    const submit = (event) => { event.preventDefault(); isEdit ? put(submitUrl) : post(submitUrl); };
    const inputClass = "h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 text-sm font-medium text-slate-800 outline-none transition focus:border-violet-400 focus:bg-white focus:ring-2 focus:ring-violet-200/50";

    return <AdminLayout><Head title={isEdit ? "Edit User" : "Tambah User"} /><div className="mx-auto max-w-4xl space-y-5"><div className="flex flex-wrap items-center gap-2 text-sm font-semibold text-slate-500"><Link href="/dashboard" className="transition hover:text-violet-600">Dashboard</Link><span className="text-slate-300">/</span><Link href="/system/access-control" className="transition hover:text-violet-600">Role & Akses</Link><span className="text-slate-300">/</span><span className="text-slate-900">{isEdit ? "Edit pengguna" : "Tambah pengguna"}</span></div><section className="relative overflow-hidden rounded-2xl border border-slate-200 bg-white p-5 shadow-sm shadow-slate-200/70 sm:p-7"><div className="pointer-events-none absolute inset-x-0 bottom-0 h-1 bg-gradient-to-r from-violet-500 via-cyan-400 to-amber-300" /><div className="flex items-start gap-3"><Link href={backUrl} className="grid h-10 w-10 shrink-0 place-items-center rounded-xl border border-slate-200 bg-slate-50 text-slate-700 transition hover:border-violet-200 hover:bg-violet-50 hover:text-violet-700"><ArrowLeft size={18} /></Link><div><div className="inline-flex items-center gap-2 rounded-lg bg-violet-50 px-3 py-1.5 text-xs font-bold text-violet-700"><UserPlus size={15} />{isEdit ? "Perbarui pengguna" : "Pengguna baru"}</div><h1 className="mt-3 text-2xl font-extrabold tracking-tight text-slate-950 sm:text-3xl">{isEdit ? userRecord.nik : "Tambah pengguna"}</h1><p className="mt-2 text-sm leading-6 text-slate-600">Lengkapi identitas login lalu tentukan role awal pengguna.</p></div></div></section><form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm shadow-slate-200/60 sm:p-6"><div className="grid gap-5 sm:grid-cols-2"><Field label="NIK / ID login" error={errors.nik}><input id="access-user-nik" name="nik" value={data.nik} onChange={(event) => setData("nik", event.target.value)} className={inputClass} required /></Field><Field label="Email" error={errors.email}><input id="access-user-email" name="email" type="email" value={data.email} onChange={(event) => setData("email", event.target.value)} className={inputClass} required /></Field><Field label="Role" error={errors.role}><select id="access-user-role" name="role" value={data.role} onChange={(event) => setData("role", event.target.value)} className={inputClass} required><option value="" disabled>Pilih role</option>{roles.map((role) => <option key={role.name} value={role.name}>{role.label}</option>)}</select></Field><div className="hidden sm:block" /><Field label={isEdit ? "Password baru (opsional)" : "Password"} error={errors.password}><input id="access-user-password" name="password" type="password" value={data.password} onChange={(event) => setData("password", event.target.value)} className={inputClass} required={!isEdit} /></Field><Field label="Konfirmasi password" error={errors.password_confirmation}><input id="access-user-password-confirmation" name="password_confirmation" type="password" value={data.password_confirmation} onChange={(event) => setData("password_confirmation", event.target.value)} className={inputClass} required={!isEdit || data.password !== ""} /></Field></div><div className="mt-7 flex flex-col-reverse gap-3 border-t border-slate-100 pt-5 sm:flex-row sm:justify-end"><Link href={backUrl} className="inline-flex h-11 items-center justify-center rounded-xl border border-slate-200 px-5 text-sm font-bold text-slate-700 transition hover:bg-slate-50">Batal</Link><button type="submit" disabled={processing} className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-violet-600 px-5 text-sm font-bold text-white shadow-sm transition hover:bg-violet-700 disabled:opacity-50"><Save size={16} />{isEdit ? "Simpan perubahan" : "Buat pengguna"}</button></div></form></div></AdminLayout>;
}
